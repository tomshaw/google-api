{
    "name": "tomshaw/google-api",
    "description": "A Laravel Google API Client.",
    "keywords": [
        "google",
        "laravel",
        "google-api",
        "google-client",
        "api-client"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "Tom Shaw",
            "email": "tomshaw@tomshaw.us",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.2|^8.3|^8.4|^8.5",
        "google/apiclient": "^2.18",
        "illuminate/session": "^10.14|^11.0|^12.0|^13.0"
    },
    "require-dev": {
        "larastan/larastan": "^3.0",
        "laravel/pint": "^1.10|^2.0",
        "mockery/mockery": "^1.6.10|^2.0",
        "nunomaduro/collision": "^8.8",
        "orchestra/testbench": "^10.0|^11.0",
        "pestphp/pest": "^4.0",
        "phpstan/phpstan": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "TomShaw\\GoogleApi\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "TomShaw\\GoogleApi\\Tests\\": "tests"
        }
    },
    "scripts": {
        "analyse": "vendor/bin/phpstan analyse",
        "baseline": "vendor/bin/phpstan analyse --ansi --memory-limit=-1 --generate-baseline --allow-empty-baseline",
        "test": "vendor/bin/pest",
        "format": "vendor/bin/pint --config pint.json"
    },
    "extra": {
        "laravel": {
            "providers": [
                "TomShaw\\GoogleApi\\Providers\\GoogleApiServiceProvider"
            ]
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        },
        "sort-packages": true,
        "process-timeout": 600
    }
}
